Display multiple keys in the table (one for each authenticated service)
Support more than 1 user
Support more than 1 service per user
Each user has her own URL where she can check all authenticated services
There is one URL to authenticate

